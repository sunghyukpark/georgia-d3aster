<div id="hazard-<%= @hazard.id %>">
  <%= @hazard.name %>
  <%= link_to 'Detail', hazard_path(@hazard) %>
  <%= link_to 'Edit', edit_hazard_path(@hazard), id: 'edit-link', remote: true %>
  <%= link_to 'Remove', @hazard, method: :delete, data:{ confirm: 'Are you sure?'}, remote: true %>
</div>